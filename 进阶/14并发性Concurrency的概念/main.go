package main

func main() {
	/*
			Go是并发的，不是并行的。
		进程（Process），简单理解为正在执行的程序。进程是系统感知程序存在的唯一标志。进程的局限是创建、撤销和切换的开销比较大。
		线程（Thread），有点是减小了程序并发执行时的开销，提高了操作系统的并发性能；缺点是线程没有自己的系统资源。
		协程（Coroutine）也叫轻量级线程，微线程。协程的调度完全由用户控制。通常将协程和子程序（函数）比较着理解。协程可以轻松创建上百万个而不会导致系统资源衰竭，而线程和进程通常最多也不能超过1万个。
	*/

}
